$(() => {

    // Select any <h1> elements, hide them first, then fade in
    $('h1').hide().fadeIn();

    // Select any <p> elements, hide them first, then fade in for 1 second
    $('p').hide().fadeIn(1000);

})